name: Build and Deploy Frontend Docker Image

on:
  push:
    branches:
      - '**'

env:
  DOCKER_IMAGE_PREFIX: reneboy/docility
  DOCKER_USERNAME: reneboy
  DOCKER_PASSWORD: Zusez3freemake12@@
  SSH_HOST: 13.211.3.64 
  SSH_USER: ubuntu
  SSH_PRIVATE_KEY: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILjLcDe78S0xnq6/Xb33J3GIwi4LlRRIrN5JVx86wijS github-deploy

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
   
      

  build:
    runs-on: ubuntu-latest
    needs: checkout  # Ensures this job runs only after the login job completes

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set Branch Variables
        id: vars
        run: |
          echo "branch_name=${GITHUB_REF#refs/heads/}" >> $GITHUB_OUTPUT
          echo "branch_type=$(echo ${GITHUB_REF#refs/heads/} | cut -d'/' -f1)" >> $GITHUB_OUTPUT
          echo "image_tag=${{ github.run_number }}" >> $GITHUB_OUTPUT

      - name: Build Docker Image
        run: |
          IMAGE_NAME=reneboy/docility-master
          IMAGE_TAG=${{ github.run_number }}
          docker build -t $IMAGE_NAME:$IMAGE_TAG .

  push-artifactory:
    runs-on: ubuntu-latest
    needs: build  
    steps:
      - name: Login to Docker Hub
        id: login
        run: |
          echo "${{ env.DOCKER_PASSWORD }}" | docker login -u "${{ env.DOCKER_USERNAME }}" --password-stdin

      - name: Push Docker Image
        run: |
          IMAGE_NAME=reneboy/docility-master
          IMAGE_TAG=${{ github.run_number }}
          docker push $IMAGE_NAME:$IMAGE_TAG

  deploy:
    runs-on: ubuntu-latest
    needs: build  # Ensures this job runs only after the build job completes
    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_PRIVATE_KEY }}
          script: |
            FRONTEND_IMAGE="reneboy/docility-master"
            FRONTEND_TAG="${{ github.run_number }}"
            FRONTEND_PORT=8080

            echo "Pulling frontend image: $FRONTEND_IMAGE:$FRONTEND_TAG"
            docker pull $FRONTEND_IMAGE:$FRONTEND_TAG

            echo "Removing existing frontend container..."
            if [ "$(docker ps -a -q -f name=frontend-app)" ]; then
              docker stop frontend-app
              docker rm frontend-app
            fi
            if [ "$(docker ps -q -f publish=$FRONTEND_PORT)" ]; then
              docker rm -f $(docker ps -q -f publish=$FRONTEND_PORT)
            fi

            echo "Starting frontend container..."
            docker run -d --name frontend-app -p $FRONTEND_PORT:80 $FRONTEND_IMAGE:$FRONTEND_TAG

            echo "Restarting Nginx..."
            sudo systemctl restart nginx

            docker ps | grep frontend-app

  cleanup:
    runs-on: ubuntu-latest
    needs: [deploy]  # Ensures this job runs after the deploy job is done
    steps:
      - name: Cleanup Docker Image
        if: always()
        run: |
          IMAGE_NAME="reneboy/docility-master"
          IMAGE_TAG="${{ github.run_number }}"
          docker rmi $IMAGE_NAME:$IMAGE_TAG || true
